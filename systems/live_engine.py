"""Live trading engine placeholder module."""

from __future__ import annotations


def run_live(tag: str, window: str) -> None:
    """Placeholder live trading runner."""
    print(f"[LIVE] Running live mode for {tag} on window {window}")

