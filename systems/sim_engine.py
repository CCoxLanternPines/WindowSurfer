"""Simulation engine placeholder module."""

from __future__ import annotations


def run_simulation(tag: str, window: str) -> None:
    """Placeholder simulation runner."""
    print(f"[SIM] Running simulation for {tag} on window {window}")

